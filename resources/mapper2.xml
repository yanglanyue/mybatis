<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.dao.ReaderMapper">
        <!--4.尚硅谷_MyBatis_接口式编程 03:30-->
        <!--不仅是配置文件和接口绑定，id和接口的相关方法名一致
        public Reader getReaderById(Integer id);
        将相关方法也绑定-->
<select id="getReaderById" resultType="r11d">
 select * from reader where id = #{id}
 </select>

    <select id="selectReader" resultType="r11d">
 select * from reader where id = #{id}
 </select>

</mapper>
